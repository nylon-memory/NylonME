[package]
name = "nylon-storage"
description = "嵌入式持久化层：WAL 预写日志 + 快照 + 崩溃恢复"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
nylon-core = { path = "../nylon-core" }
nylon-graph = { path = "../nylon-graph" }
